Функция вызывается в цикле
Вункиця использует статическую переменную - сдвиг по файлу
Читаем строки пока не EOF

+ Написать фукнцию которая просто читает строку из файла
+ Написать Функцию которая читает файл построчно
+ Чтение в буфере 
- Ввести статическую переменную
- Сохранять "Остаток" в переменной и переключать чтение
    - Мы считали в буфер
    - Переводим в line все до \n

https://www.notion.so/coding_wiki-1d8b8bc675f5426db90a02dd22324ac8